// Create a function named counter() that has one argument that gets
// the value and outputs it into the console.


function counter(value)
{
    console.log(value);
}
const mynewvalue = 80;
counter(mynewvalue);